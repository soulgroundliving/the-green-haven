# Restore Drill Log

Append a row each time `tools/restore-drill.sh` runs. Schedule next drill 3 months from the previous successful one.

| Date (BKK) | Operator | Backup used | Wall-clock time | Result | Notes |
|------------|----------|-------------|-----------------|--------|-------|
| _(no drills run yet)_ | — | — | — | — | First drill scheduled — see STAGING_RUNBOOK.md Step 1-4 to set up the staging project, then run `tools/restore-drill.sh`. |
